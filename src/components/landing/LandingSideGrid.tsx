/** Full-bleed faded line grid — side gutters for landing bands (mirrors hero). */
export function LandingSideGrid() {
  return (
    <div
      className="landing-side-grid pointer-events-none absolute inset-0 left-1/2 z-0 w-screen max-w-[100vw] -translate-x-1/2"
      aria-hidden
    />
  );
}
